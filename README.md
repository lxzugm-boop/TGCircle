# Telegram Circle Video Bot (Render + Docker)

Этот вариант проекта настроен под деплой на Render в режиме **Docker** (а не Python).

## Что внутри

- `bot.py` — логика бота (принимает видео → возвращает кружочек)
- `requirements.txt` — зависимости (`aiogram`, `python-dotenv`)
- `Dockerfile` — образ на базе `python:3.12-slim` + установка `ffmpeg`
- Этот `README.md`

## Локальный запуск

```bash
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

export BOT_TOKEN=123456:ABC-DEF...  # Windows: set BOT_TOKEN=...
python bot.py
```

## Деплой на Render (Docker)

1. Залей папку с этими файлами в GitHub.

2. В Render:
   - Нажми **New → Web Service**
   - Выбери репозиторий
   - В поле **Environment** выбери: `Docker` (НЕ Python)

3. Остальные поля:

   - Build Command: **оставь пустым** (Render сам соберёт Dockerfile)
   - Start Command: **пусто** (используется `CMD` из Dockerfile)

4. В разделе **Environment → Env Vars** добавь переменную:

   - `BOT_TOKEN` = токен от BotFather

   (опционально):
   - `VIDEO_MAX_DURATION` — макс. длительность ролика в секундах (по умолчанию 90)
   - `FFMPEG_BIN` — путь до `ffmpeg` (по умолчанию `ffmpeg`)

5. Нажми **Deploy**.  
   Render соберёт Docker-образ, внутри которого уже будет стоять `ffmpeg`, и запустит бота.

### Проверка

- В логах Render увидишь `Starting bot polling...`
- В Телеграме:
  - `/start`
  - Отправь обычное видео
  - Получишь кружочек 🟣

Если в логах будут ошибки — просто скинь их мне, разберём.
